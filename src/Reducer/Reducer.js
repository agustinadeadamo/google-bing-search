/**
 * @desc Actions
 */
  
/**
 * @desc Initial State
 */
const InitialState = {};
  
/**
 * @desc Reducer.
 *
 * @param { Object } state
 * @param { Object } action
 *
 * @return { Object }
 */
const Reducer = (state = InitialState, action) => {
switch (action.type) {
    default:
    return state;
}
};

export default Reducer;
  